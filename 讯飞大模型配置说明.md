# 讯飞星火大模型配置说明

## 📝 配置步骤

### 1. 编辑环境变量文件

```bash
cd /opt/ppt/web/backend
vim .env
```

### 2. 配置讯飞大模型参数

在 `.env` 文件中添加以下配置：

```bash
# LLM 提供商（使用 OpenAI 兼容模式）
LLM_PROVIDER=openai_compatible

# 讯飞星火大模型配置
OPENAI_COMPATIBLE_API_KEY=96ce545849f922aada14f28f8573dffd:YzM5OWQzMmYzMzE0NDllNWMwYTgxY2Q3
OPENAI_COMPATIBLE_BASE_URL=https://maas-coding-api.cn-huabei-1.xf-yun.com/v2
OPENAI_COMPATIBLE_MODEL=astron-code-latest
```

### 3. 重启服务

```bash
cd /opt/ppt/web
docker-compose restart backend celery-worker
```

### 4. 验证配置

```bash
# 查看后端日志，确认 LLM 配置成功
docker-compose logs backend | grep -i llm

# 应该看到类似输出：
# ✓ LLM provider configured: openai_compatible
```

---

## 🔐 安全说明

### API Key 格式

讯飞星火的 API Key 格式为：`AppID:APISecret`

```
96ce545849f922aada14f28f8573dffd:YzM5OWQzMmYzMzE0NDllNWMwYTgxY2Q3
└─────────────┬─────────────┘ └──────────────┬──────────────┘
            AppID                        APISecret
```

### 安全保护

✅ **已保护**：
- `.env` 文件已在 `.gitignore` 中，不会上传到 GitHub
- 密钥只存储在服务器本地
- Docker 容器内部使用环境变量传递

❌ **不要做**：
- 不要将 `.env` 文件提交到 Git
- 不要在代码中硬编码 API Key
- 不要在公开文档中暴露密钥

---

## 🔧 配置参数说明

### LLM_PROVIDER
- **值**: `openai_compatible`
- **说明**: 使用 OpenAI 兼容模式，支持讯飞星火等第三方大模型

### OPENAI_COMPATIBLE_API_KEY
- **值**: `96ce545849f922aada14f28f8573dffd:YzM5OWQzMmYzMzE0NDllNWMwYTgxY2Q3`
- **格式**: `AppID:APISecret`
- **说明**: 讯飞星火的认证密钥

### OPENAI_COMPATIBLE_BASE_URL
- **值**: `https://maas-coding-api.cn-huabei-1.xf-yun.com/v2`
- **说明**: 讯飞星火 API 的基础 URL

### OPENAI_COMPATIBLE_MODEL
- **值**: `astron-code-latest`
- **说明**: 使用的模型 ID（讯飞星火代码模型）

---

## 🧪 测试配置

### 方法 1: 通过 API 测试

```bash
# 发送测试消息
curl -X POST http://localhost:8000/api/chat/message \
  -H "Content-Type: application/json" \
  -d '{
    "session_id": "test-session",
    "message": "你好，请介绍一下你自己"
  }'
```

### 方法 2: 通过前端测试

1. 访问 `http://your-server-ip`
2. 创建新会话
3. 发送消息测试 AI 响应

---

## 🐛 常见问题

### 1. LLM 功能未启用

**症状**: 日志显示 "LLM features will be disabled"

**解决**:
```bash
# 检查环境变量是否正确配置
docker-compose exec backend env | grep OPENAI_COMPATIBLE

# 确保三个参数都已配置
# OPENAI_COMPATIBLE_API_KEY
# OPENAI_COMPATIBLE_BASE_URL
# OPENAI_COMPATIBLE_MODEL
```

### 2. API 调用失败

**症状**: 返回 401 或 403 错误

**解决**:
- 检查 API Key 格式是否正确（AppID:APISecret）
- 确认 API Key 是否有效
- 检查网络是否能访问讯飞 API

### 3. 模型不存在

**症状**: 返回 "model not found" 错误

**解决**:
- 确认模型 ID 是否正确：`astron-code-latest`
- 检查账号是否有该模型的访问权限

---

## 📊 配置对比

### OpenAI 官方 API
```bash
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-your-openai-key
OPENAI_MODEL=gpt-4-turbo-preview
```

### 讯飞星火大模型（推荐）
```bash
LLM_PROVIDER=openai_compatible
OPENAI_COMPATIBLE_API_KEY=96ce545849f922aada14f28f8573dffd:YzM5OWQzMmYzMzE0NDllNWMwYTgxY2Q3
OPENAI_COMPATIBLE_BASE_URL=https://maas-coding-api.cn-huabei-1.xf-yun.com/v2
OPENAI_COMPATIBLE_MODEL=astron-code-latest
```

### Anthropic Claude
```bash
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-your-key
ANTHROPIC_MODEL=claude-3-opus-20240229
```

---

## 🔄 切换模型

如果需要切换到其他模型，只需修改 `.env` 文件：

```bash
# 1. 编辑配置
vim /opt/ppt/web/backend/.env

# 2. 修改 LLM_PROVIDER 和对应的配置

# 3. 重启服务
cd /opt/ppt/web
docker-compose restart backend celery-worker
```

---

**文档版本**: 1.0  
**最后更新**: 2026-04-30  
**适用模型**: 讯飞星火 astron-code-latest

⚠️ **重要提示**: 本文档包含敏感信息，请勿上传到公开仓库！
