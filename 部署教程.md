# PPT Master Web - 服务器部署教程

## 📋 环境要求

- **操作系统**: Ubuntu 20.04+ / CentOS 7+ / Debian 10+
- **CPU**: 2 核（推荐 4 核）
- **内存**: 4GB（推荐 8GB）
- **硬盘**: 20GB（推荐 50GB）
- **软件**: Docker 20.10+, Docker Compose 2.0+, Git

---

## 🚀 首次部署

### 1. 安装 Docker 和 Docker Compose

```bash
# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 启动 Docker
sudo systemctl start docker
sudo systemctl enable docker

# 安装 Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 验证安装
docker --version
docker-compose --version
```

### 2. 配置 Docker 镜像源（可选，加速镜像拉取）

```bash
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json > /dev/null <<EOF
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com",
    "https://mirror.baidubce.com"
  ]
}
EOF

sudo systemctl daemon-reload
sudo systemctl restart docker
```

### 3. 从 GitHub 克隆项目

```bash
# 克隆项目到服务器
cd /opt
sudo git clone https://github.com/starmortal/ppt.git
cd ppt/web

# 如果需要切换到特定分支
# git checkout main
```

### 4. 配置环境变量

```bash
# 进入 web 目录
cd /opt/ppt/web

# 复制环境变量模板
cp .env.example .env

# 编辑环境变量
sudo vim .env
```

**必须配置的项**：

```bash
# 应用密钥（必须修改，生产环境使用强密钥）
SECRET_KEY=your-very-secure-random-secret-key-here

# 数据库密码（建议修改）
POSTGRES_PASSWORD=your_secure_db_password

# MinIO 对象存储密码（建议修改，用于存储上传的文件和生成的PPT）
# 注意：这四个值必须保持一致（用户名对用户名，密码对密码）
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=your_secure_minio_password
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=your_secure_minio_password

# LLM 配置（推荐使用讯飞星火大模型）
LLM_PROVIDER=openai_compatible

# 讯飞星火大模型配置（必须填写）
OPENAI_COMPATIBLE_API_KEY=your-xunfei-api-key
OPENAI_COMPATIBLE_BASE_URL=https://maas-coding-api.cn-huabei-1.xf-yun.com/v2
OPENAI_COMPATIBLE_MODEL=astron-code-latest

# 或使用 OpenAI（可选）
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-your-openai-api-key-here
# OPENAI_MODEL=gpt-4-turbo-preview

# 或使用 Anthropic Claude（可选）
# LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
# ANTHROPIC_MODEL=claude-3-opus-20240229
```

**讯飞星火 API Key 格式说明**：
- 格式：`AppID:APISecret`
- 示例：`96ce545849f922aada14f28f8573dffd:YzM5OWQzMmYzMzE0NDllNWMwYTgxY2Q3`
- 获取方式：登录讯飞开放平台获取

**MinIO 对象存储说明**：

MinIO 是一个开源的对象存储服务（类似 AWS S3），用于存储：
- 用户上传的源文件（PDF、DOCX、图片等）
- AI 生成的图片
- 最终生成的 PPT 文件

**MinIO 密码配置**：
- MinIO 有两组密码需要配置，它们必须保持一致：
  1. **MinIO 服务器密码**：
     - `MINIO_ROOT_USER`: MinIO 服务器的管理员用户名
     - `MINIO_ROOT_PASSWORD`: MinIO 服务器的管理员密码
  2. **应用连接密码**：
     - `MINIO_ACCESS_KEY`: 应用连接 MinIO 使用的用户名（必须与 MINIO_ROOT_USER 一致）
     - `MINIO_SECRET_KEY`: 应用连接 MinIO 使用的密码（必须与 MINIO_ROOT_PASSWORD 一致）

**如何修改 MinIO 密码**：

如果你想修改默认的 `minioadmin` 密码（强烈建议生产环境修改），在 `.env` 文件中同时修改这四个值：

```bash
MINIO_ROOT_USER=your_custom_username
MINIO_ROOT_PASSWORD=your_custom_password
MINIO_ACCESS_KEY=your_custom_username      # 与 MINIO_ROOT_USER 一致
MINIO_SECRET_KEY=your_custom_password      # 与 MINIO_ROOT_PASSWORD 一致
```

**注意**：修改密码后需要重新启动服务：
```bash
sudo docker-compose down
sudo docker-compose up -d
```

**生成随机密钥**：
```bash
# 生成 SECRET_KEY
python3 -c "import secrets; print(secrets.token_urlsafe(32))"

# 生成数据库密码
python3 -c "import secrets; print(secrets.token_urlsafe(24))"

# 生成 MinIO 密码
python3 -c "import secrets; print(secrets.token_urlsafe(16))"
```

### 5. 启动服务

```bash
# 返回 web 目录
cd /opt/ppt/web

# 启动所有服务（首次启动需要 5-10 分钟）
sudo docker-compose up -d

# 查看启动日志
sudo docker-compose logs -f
```

### 6. 验证部署

```bash
# 检查服务状态
sudo docker-compose ps

# 应该看到所有服务都在运行：
# pptmaster-postgres      Up (healthy)
# pptmaster-redis         Up (healthy)
# pptmaster-minio         Up (healthy)
# pptmaster-backend       Up
# pptmaster-celery-worker Up
# pptmaster-frontend      Up
# pptmaster-nginx         Up

# 测试后端健康检查
curl http://localhost:8000/health

# 测试前端
curl http://localhost
```

### 7. 配置防火墙

```bash
# Ubuntu/Debian (使用 ufw)
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable

# CentOS (使用 firewalld)
sudo firewall-cmd --permanent --add-service=ssh
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

### 8. 访问应用

在浏览器中访问：
- **前端**: `http://your-server-ip`
- **API 文档**: `http://your-server-ip:8000/docs`

---

## 🔄 更新部署

当 GitHub 仓库有更新时，按以下步骤更新：

### 方法 1: 快速更新（推荐）

```bash
# 进入项目目录
cd /opt/ppt

# 拉取最新代码
sudo git pull origin main

# 重启服务（会自动重新构建）
cd web
sudo docker-compose down
sudo docker-compose up -d --build

# 查看日志确认启动成功
sudo docker-compose logs -f
```

### 方法 2: 完整更新（包含数据库迁移）

```bash
# 1. 停止服务
cd /opt/ppt/web
sudo docker-compose down

# 2. 备份数据（重要！）
sudo docker-compose exec postgres pg_dump -U pptmaster pptmaster > backup_$(date +%Y%m%d).sql

# 3. 拉取最新代码
cd /opt/ppt
sudo git pull origin main

# 4. 检查环境变量是否有新增配置
cd web/backend
diff .env.example .env
# 如果有新配置项，手动添加到 .env

# 5. 重新构建并启动
cd /opt/ppt/web
sudo docker-compose up -d --build

# 6. 查看日志
sudo docker-compose logs -f
```

### 方法 3: 仅更新前端或后端

```bash
# 只更新后端
cd /opt/ppt/web
sudo docker-compose up -d --build backend celery-worker

# 只更新前端
sudo docker-compose up -d --build frontend
```

---

## 🔧 常用管理命令

### 服务管理

```bash
cd /opt/ppt/web

# 启动服务
sudo docker-compose up -d

# 停止服务
sudo docker-compose down

# 重启服务
sudo docker-compose restart

# 重启特定服务
sudo docker-compose restart backend

# 查看服务状态
sudo docker-compose ps

# 查看日志
sudo docker-compose logs -f

# 查看特定服务日志
sudo docker-compose logs -f backend
sudo docker-compose logs -f frontend
```

### 数据备份

```bash
# 备份数据库
cd /opt/ppt/web
sudo docker-compose exec postgres pg_dump -U pptmaster pptmaster > backup_$(date +%Y%m%d).sql

# 恢复数据库
sudo docker-compose exec -T postgres psql -U pptmaster pptmaster < backup_20260430.sql

# 备份整个项目
cd /opt
sudo tar -czf ppt-backup-$(date +%Y%m%d).tar.gz ppt/
```

### 清理资源

```bash
# 清理未使用的 Docker 镜像
sudo docker image prune -a

# 清理未使用的容器
sudo docker container prune

# 清理未使用的卷
sudo docker volume prune

# 清理所有未使用资源
sudo docker system prune -a --volumes
```

---

## 🐛 常见问题

### 1. 服务无法启动

**检查日志**：
```bash
sudo docker-compose logs backend
sudo docker-compose logs frontend
```

**常见原因**：
- `.env` 文件未配置
- 端口被占用（检查 `netstat -tulpn | grep -E "80|8000|5432|6379"`）
- 内存不足（检查 `free -h`）

### 2. 数据库连接失败

```bash
# 检查 PostgreSQL 状态
sudo docker-compose ps postgres

# 查看 PostgreSQL 日志
sudo docker-compose logs postgres

# 重启 PostgreSQL
sudo docker-compose restart postgres
```

### 3. 前端无法访问后端

**检查 Nginx 配置**：
```bash
sudo docker-compose exec nginx nginx -t
```

**检查端口监听**：
```bash
netstat -tulpn | grep :80
```

### 4. Git 拉取失败

```bash
# 如果有本地修改冲突
cd /opt/ppt
sudo git stash  # 暂存本地修改
sudo git pull origin main
sudo git stash pop  # 恢复本地修改

# 或者强制覆盖本地修改
sudo git fetch origin
sudo git reset --hard origin/main
```

### 5. 磁盘空间不足

```bash
# 查看磁盘使用
df -h

# 清理 Docker 资源
sudo docker system prune -a --volumes

# 清理日志
sudo find /var/log -name "*.log" -mtime +30 -delete
```

---

## 🔐 安全建议

### 生产环境必须修改

1. **SECRET_KEY**: 使用随机生成的强密钥
2. **数据库密码**: 使用强密码（至少 16 位）
3. **MinIO 密码**: 使用强密码
4. **DEBUG**: 在 `.env` 中设置为 `False`

### 配置 HTTPS（推荐）

```bash
# 安装 Certbot
sudo apt install -y certbot python3-certbot-nginx

# 获取 SSL 证书
sudo certbot certonly --standalone -d your-domain.com

# 证书会保存在 /etc/letsencrypt/live/your-domain.com/

# 修改 nginx/nginx.conf 添加 HTTPS 配置
# 然后重启 nginx
sudo docker-compose restart nginx
```

### 定期备份

```bash
# 创建备份脚本
sudo vim /opt/backup-ppt.sh
```

添加内容：
```bash
#!/bin/bash
BACKUP_DIR="/opt/backups"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR

# 备份数据库
cd /opt/ppt/web
docker-compose exec -T postgres pg_dump -U pptmaster pptmaster > $BACKUP_DIR/db_$DATE.sql

# 备份文件
tar -czf $BACKUP_DIR/files_$DATE.tar.gz /opt/ppt/web/

# 删除 7 天前的备份
find $BACKUP_DIR -name "*.sql" -mtime +7 -delete
find $BACKUP_DIR -name "*.tar.gz" -mtime +7 -delete

echo "Backup completed: $DATE"
```

设置定时任务：
```bash
sudo chmod +x /opt/backup-ppt.sh

# 添加到 crontab (每天凌晨 2 点备份)
sudo crontab -e

# 添加以下行
0 2 * * * /opt/backup-ppt.sh >> /var/log/ppt-backup.log 2>&1
```

---

## 📞 获取帮助

- **GitHub 仓库**: https://github.com/starmortal/ppt
- **提交 Issue**: https://github.com/starmortal/ppt/issues
- **查看文档**: 项目 README.md

---

**文档版本**: 1.0  
**最后更新**: 2026-04-30  
**GitHub**: https://github.com/starmortal/ppt

🎉 **部署完成，开始使用 PPT Master Web！**
