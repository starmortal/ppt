# PPT Master Web 本地开发环境搭建教程

本教程将指导你在本地电脑上搭建和运行 PPT Master Web 项目的完整开发环境。

## 前置要求

在开始之前，请确保你的电脑已安装以下软件：

### 必需软件

1. **Git**
   - 用于克隆项目代码
   - 下载：https://git-scm.com/downloads

2. **Docker Desktop**
   - 用于运行所有服务（数据库、Redis、MinIO、后端、前端）
   - Windows/Mac 下载：https://www.docker.com/products/docker-desktop
   - 安装后启动 Docker Desktop

3. **Python 3.11+**（可选，用于直接运行 PPT 生成脚本）
   - 下载：https://www.python.org/downloads/
   - 安装时勾选 "Add Python to PATH"

4. **Node.js 18+**（可选，用于前端开发）
   - 下载：https://nodejs.org/
   - 推荐下载 LTS 版本

### 可选软件

- **VS Code** 或其他代码编辑器
- **Postman** 或 **Insomnia**（用于测试 API）

---

## 快速开始（使用 Docker）

### 1. 克隆项目

```bash
# 克隆项目到本地
git clone https://github.com/starmortal/ppt.git

# 进入项目目录
cd ppt/web
```

### 2. 配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env

# 使用文本编辑器打开 .env 文件
# Windows: notepad .env
# Mac: open -e .env
# Linux: nano .env
```

**必须配置的环境变量：**

```bash
# 应用密钥（生成一个随机字符串）
SECRET_KEY=your-random-secret-key-here

# 数据库密码（可以保持默认或修改）
POSTGRES_PASSWORD=pptmaster_password

# MinIO 对象存储密码（四个值必须保持一致）
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# LLM 配置（使用讯飞星火大模型）
LLM_PROVIDER=openai_compatible
OPENAI_COMPATIBLE_API_KEY=你的讯飞API密钥
OPENAI_COMPATIBLE_BASE_URL=https://maas-coding-api.cn-huabei-1.xf-yun.com/v2
OPENAI_COMPATIBLE_MODEL=astron-code-latest
```

**生成随机密钥：**

```bash
# 生成 SECRET_KEY（在命令行运行）
python -c "import secrets; print(secrets.token_urlsafe(32))"

# 或者使用在线工具：https://generate-secret.vercel.app/32
```

**获取讯飞星火 API Key：**
1. 访问讯飞开放平台：https://www.xfyun.cn/
2. 注册并登录
3. 创建应用获取 AppID 和 APISecret
4. API Key 格式：`AppID:APISecret`
5. 示例：`96ce545849f922aada14f28f8573dffd:YzM5OWQzMmYzMzE0NDllNWMwYTgxY2Q3`

### 3. 启动所有服务

```bash
# 启动所有服务（首次启动需要 5-10 分钟下载镜像和构建）
docker-compose up -d

# 查看启动日志
docker-compose logs -f

# 按 Ctrl+C 退出日志查看
```

### 4. 验证服务状态

```bash
# 检查所有服务是否正常运行
docker-compose ps

# 应该看到以下服务都在运行：
# pptmaster-postgres      Up (healthy)
# pptmaster-redis         Up (healthy)
# pptmaster-minio         Up (healthy)
# pptmaster-backend       Up
# pptmaster-celery-worker Up
# pptmaster-frontend      Up
# pptmaster-nginx         Up
```

### 5. 访问应用

打开浏览器访问：

- **前端界面**：http://localhost
- **后端 API 文档**：http://localhost:8000/docs
- **MinIO 控制台**：http://localhost:9001
  - 用户名：minioadmin
  - 密码：minioadmin（或你在 .env 中设置的密码）

---

## 本地开发模式（不使用 Docker）

如果你想在本地直接运行代码进行开发（不使用 Docker），请按照以下步骤操作。

### 1. 安装依赖服务

你需要手动安装并运行以下服务：

**PostgreSQL 数据库：**
```bash
# Windows: 下载安装包
# https://www.postgresql.org/download/windows/

# Mac (使用 Homebrew):
brew install postgresql@15
brew services start postgresql@15

# 创建数据库
createdb pptmaster
```

**Redis：**
```bash
# Windows: 下载安装包
# https://github.com/microsoftarchive/redis/releases

# Mac (使用 Homebrew):
brew install redis
brew services start redis
```

**MinIO：**
```bash
# Windows/Mac: 下载二进制文件
# https://min.io/download

# 启动 MinIO
minio server ./minio-data --console-address ":9001"
```

### 2. 后端开发

```bash
# 进入后端目录
cd backend

# 创建 Python 虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 复制并配置环境变量
cp .env.example .env
# 编辑 .env 文件，配置数据库、Redis、MinIO 连接信息

# 运行数据库迁移（如果有）
# alembic upgrade head

# 启动后端服务
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 在另一个终端启动 Celery Worker
celery -A app.celery_app worker --loglevel=info
```

后端 API 将在 http://localhost:8000 运行

### 3. 前端开发

```bash
# 打开新终端，进入前端目录
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端将在 http://localhost:5173 运行

---

## 常用命令

### Docker 相关

```bash
# 启动所有服务
docker-compose up -d

# 停止所有服务
docker-compose down

# 重启所有服务
docker-compose restart

# 查看服务日志
docker-compose logs -f [service_name]
# 例如：docker-compose logs -f backend

# 重新构建并启动（代码更新后）
docker-compose up -d --build

# 进入容器内部
docker-compose exec backend bash
docker-compose exec frontend sh

# 清理所有数据（谨慎使用！）
docker-compose down -v
```

### 数据库相关

```bash
# 连接到 PostgreSQL
docker-compose exec postgres psql -U pptmaster -d pptmaster

# 备份数据库
docker-compose exec postgres pg_dump -U pptmaster pptmaster > backup.sql

# 恢复数据库
docker-compose exec -T postgres psql -U pptmaster pptmaster < backup.sql
```

### 查看 MinIO 文件

```bash
# 访问 MinIO 控制台
# http://localhost:9001
# 用户名：minioadmin
# 密码：minioadmin
```

---

## 故障排除

### 问题 1：Docker 启动失败

**症状**：`docker-compose up` 报错

**解决方案**：
1. 确保 Docker Desktop 正在运行
2. 检查端口是否被占用（80, 8000, 5432, 6379, 9000, 9001）
3. 尝试清理并重新启动：
   ```bash
   docker-compose down
   docker-compose up -d --build
   ```

### 问题 2：前端无法连接后端

**症状**：前端显示网络错误

**解决方案**：
1. 检查后端是否正常运行：`docker-compose logs backend`
2. 访问 http://localhost:8000/docs 确认 API 可访问
3. 检查浏览器控制台的错误信息

### 问题 3：AI 不回复或报错

**症状**：发送消息后 AI 没有回复

**解决方案**：
1. 检查 `.env` 文件中的 LLM 配置是否正确
2. 确认讯飞 API Key 有效且有余额
3. 查看后端日志：`docker-compose logs -f backend`
4. 测试 API 连接：
   ```bash
   curl -X POST http://localhost:8000/api/chat/message \
     -H "Content-Type: application/json" \
     -d '{"session_id":"test","message":"你好"}'
   ```

### 问题 4：文件上传失败

**症状**：上传文件时报错

**解决方案**：
1. 检查 MinIO 是否正常运行：`docker-compose ps minio`
2. 访问 MinIO 控制台确认 bucket 已创建：http://localhost:9001
3. 检查 `.env` 中的 MinIO 配置是否正确

### 问题 5：数据库连接失败

**症状**：后端启动时报数据库连接错误

**解决方案**：
1. 确认 PostgreSQL 容器正常运行：`docker-compose ps postgres`
2. 检查 `.env` 中的数据库密码是否与 docker-compose.yml 一致
3. 等待数据库完全启动（首次启动需要时间）
4. 重启后端服务：`docker-compose restart backend`

---

## 开发建议

### 1. 代码热重载

- **后端**：使用 `--reload` 参数启动 uvicorn，代码修改后自动重启
- **前端**：`npm run dev` 自动支持热重载

### 2. 调试技巧

**后端调试：**
```python
# 在代码中添加断点
import pdb; pdb.set_trace()

# 或使用 VS Code 的调试功能
```

**前端调试：**
- 使用浏览器开发者工具（F12）
- 查看 Console 和 Network 标签
- 使用 React DevTools 扩展

### 3. 日志查看

```bash
# 实时查看所有服务日志
docker-compose logs -f

# 只查看特定服务
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f celery-worker
```

### 4. 性能优化

- 使用 Docker 的 BuildKit 加速构建：
  ```bash
  DOCKER_BUILDKIT=1 docker-compose build
  ```
- 前端生产构建：
  ```bash
  cd frontend
  npm run build
  ```

---

## 项目结构

```
web/
├── backend/              # 后端 FastAPI 应用
│   ├── app/
│   │   ├── api/         # API 路由
│   │   ├── agents/      # AI Agent（Strategist, Executor 等）
│   │   ├── core/        # 核心配置（LLM 客户端等）
│   │   ├── models/      # 数据模型
│   │   └── services/    # 业务服务
│   ├── Dockerfile
│   ├── requirements.txt
│   └── .env.example
├── frontend/            # 前端 React 应用
│   ├── src/
│   │   ├── pages/      # 页面组件
│   │   ├── services/   # API 服务
│   │   └── App.tsx
│   ├── Dockerfile
│   └── package.json
├── nginx/              # Nginx 配置
├── docker-compose.yml  # Docker 编排配置
├── .env.example       # 环境变量模板
└── 本地开发环境搭建教程.md  # 本文档
```

---

## 下一步

1. **熟悉项目**：浏览代码，了解项目结构
2. **阅读文档**：查看 `../skills/ppt-master/SKILL.md` 了解 PPT 生成流程
3. **测试功能**：在前端界面尝试生成 PPT
4. **开发新功能**：根据需求添加新的 API 或前端组件

---

## 获取帮助

- **GitHub Issues**：https://github.com/starmortal/ppt/issues
- **项目文档**：查看 `web/` 目录下的其他 Markdown 文件
- **API 文档**：http://localhost:8000/docs（启动后访问）

---

## 更新日志

- **2026-05-01**：初始版本，包含 Docker 和本地开发两种方式
